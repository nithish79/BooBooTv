services:
  open-iptv:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: open-iptv
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
